<h3>Supervisor Dashboard</h3>

<%= form_for :tenants,url: tenants_path,  method: :get do |f| %>
<div class="input-group input-group-sm">
  <%= label_tag :keywords, nil, class: "sr-only" %>
  <%= text_field_tag :keywords,nil, placehoder: "Apartment Number, ID", class: "form-control input-sm" %>
  <span class="input-group-btn">
    <%= submit_tag "Find Tenant", class: "btn btn-primary btn-sm" %>
  </span>
</div>
<% end %>

<%= render "dashboards/shared/supervisor_view" %>
