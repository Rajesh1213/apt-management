<p id="notice"><%= notice %></p>

<div class="col-md-12">
  <h3>Apartments Overview</h3>
  <h4>Total Apts: <%= @apartments.count %></h4>
</div>
<div class="col-md-12">
  <table class="table table-striped table-hover table-condensed">
    <thead>
      <tr>
        <th>Apt ID</th>
        <th>Type</th>
        <th>Status</th>
        <th>Tenant Info</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @apartments.each do |apartment| %>
      <tr>
        <td><%= apartment.id %></td>
        <td><%= apartment.apartment_type %></td>
        <td><%= apartment.status %> </td>
        <td>
          <span class="tenant-name"><%= apartment.tenants.last.full_name %> / Lease Ends: <%= apartment.rentals.last.lease_end %></span><br/>
          <span class="label label-sm label-default">jane</span> <span class="label label-sm label-default">jane@mail.com</span> </td>
        <td>
          <td><%= link_to 'Show', apartment %></td>
          <td><%= link_to 'Edit', edit_apartment_path(apartment) %></td>
          <td><%= link_to 'Destroy', apartment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
</div>


<%= link_to 'New Apartment', new_apartment_path %>
