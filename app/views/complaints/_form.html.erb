<%= form_for(@complaint) do |f| %>
  <% if @complaint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@complaint.errors.count, "error") %> prohibited this complaint from being saved:</h2>

      <ul>
      <% @complaint.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :complaint_date %><br>
    <%= f.date_select :complaint_date %>
  </div>
  <div class="field">
    <%= f.label :rental_complaint %><br>
    <%= f.text_area :rental_complaint %>
  </div>
  <div class="field">
    <%= f.label :apt_complaint %><br>
    <%= f.text_area :apt_complaint %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
